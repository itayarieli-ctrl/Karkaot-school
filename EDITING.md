# איך עורכים את הטקסטים באתר

> **קצר**: יש שתי דרכים לעדכן טקסטים — **עורך מקוון (מומלץ)** או **עריכה
> ידנית ב-GitHub**. שתיהן מעדכנות את אותו קובץ (`content/copy.json`)
> ולכן בטוחות להחליף ביניהן בכל זמן.

---

## דרך A — עורך מקוון בכתובת `/admin` (מומלץ)

זוהי הדרך הקלה לעדכן טקסטים בלי לגעת ב-JSON או ב-GitHub.

### 1. גש לעורך

נכנס אל **https://[הדומיין שלך]/admin** (אחרי דפלוי ל-Vercel — `school.ishadama.co.il/admin` או דומה).

הדפדפן יבקש שם משתמש וסיסמה — אלה הערכים שהגדרת ב-Vercel בתור
`ADMIN_USERNAME` ו-`ADMIN_PASSWORD`.

### 2. ערוך טקסטים בטופס

תראה טופס מאורגן לפי סקציות (Hero, Services, Syllabus, FAQ, וכו'). פתח
את הסקציה הרלוונטית, ערוך את הטקסט בשדות. אתה יכול לערוך כמה סקציות
במכה אחת.

יש גם כפתורים להוסיף/להסיר פריטים (שיעור חדש, וובינר חדש, שאלת FAQ נוספת).

### 3. לחץ "פרסום"

הכפתור הירוק בראש העמוד. ברגע שתלחץ:
- העורך שולח את כל הטקסטים החדשים לשרת
- השרת מבצע commit ל-GitHub
- Vercel מזהה את ה-commit ובונה גרסה חדשה של האתר
- תוך 60-90 שניות — האתר חי עם הטקסטים החדשים

**הערה**: אם הכפתור אפור = אין שינויים לפרסם. עשה שינוי כלשהו ויהפוך לירוק.

### תקלות אפשריות

- **"GITHUB_TOKEN env var not set"** — לא הוגדר ה-token של GitHub ב-Vercel.
  ראה הוראות בקובץ `.env.example` או פנה אליי.
- **"GitHub commit failed: HTTP 404"** — ה-token פג תוקף או נמחק. צריך
  להפיק חדש.
- **שינוי לא מופיע אחרי 2 דקות** — בדוק ב-Vercel Dashboard אם ה-build
  עבר בהצלחה.

---

## דרך B — עריכה ידנית ב-GitHub

אם משום מה העורך לא עובד או שאתה מעדיף ידני — אפשר לערוך את `copy.json`
ישירות ב-GitHub. שתי הדרכים מעדכנות את אותו מקור אמת.

### עריכת טקסט — שלב אחר שלב

### 1. פתח את הקובץ ב-GitHub

לך לקישור הזה:

> **[content/copy.json](https://github.com/itayarieli-ctrl/Karkaot-school/blob/claude/connect-google-drive-D4wfP/content/copy.json)**

(אם מבקש להתחבר — תכנס לחשבון GitHub שלך)

### 2. לחץ על העיפרון לעריכה

בפינה הימנית-עליונה של הקובץ יש אייקון של עיפרון ✏️. לחץ עליו.

### 3. ערוך טקסט

תראה את כל הטקסטים מאורגנים לפי סקציה (Hero, Services, Syllabus, FAQ, וכו').
**ערוך רק טקסט שמופיע בתוך מירכאות** `"..."`.

**דוגמה — שינוי המחיר ב-FAQ:**

```json
"q": "מתי הקורס נפתח? כמה הוא עולה?",
"a": "הקורס הוא אישי – פתוח וזמין בזמן שמתאים לכם. העלות היא 5,500 ש״ח, ויש הנחה למשרתי מילואים פעילים."
```

אם רוצים לשנות ל-6,000 ש"ח, פשוט תחליף את הטקסט בין המירכאות:

```json
"q": "מתי הקורס נפתח? כמה הוא עולה?",
"a": "הקורס הוא אישי – פתוח וזמין בזמן שמתאים לכם. העלות היא 6,000 ש״ח, ויש הנחה למשרתי מילואים פעילים."
```

### 4. שמור

תגלול לתחתית העמוד. תראה אזור "Commit changes".

- בשורת הכותרת, כתוב משפט קצר כמו: `עדכון מחיר ב-FAQ`
- לחץ על הכפתור הירוק **"Commit changes"**

### 5. האתר מתעדכן אוטומטית

תוך כ-60-90 שניות Vercel יבנה גרסה חדשה של האתר עם השינוי שלך. בלי לעשות
כלום נוסף. נכנס לאתר → מרענן → רואה את השינוי.

---

## חוקים חשובים — אל תעבור עליהם

### ❌ אל תמחק או תזיז מירכאות, פסיקים, סוגריים

זה JSON. אם תפיל פסיק או סוגר — האתר יישבר. תיקון:
- אם שכחת פסיק או מירכאה, ה-build ייכשל (תראה את זה ב-Vercel)
- אז פשוט תחזור לקובץ ב-GitHub ותתקן (או תשחזר את הגרסה הקודמת — ראו "שחזור" למטה)

### ❌ אל תשנה את המפתחות (השמות באנגלית)

`"title"`, `"subtitle"`, `"badge"`, `"cta"` — אלה השמות שהקוד מצפה להם.
**שנה רק את הערך אחרי הנקודתיים**, לא את השם לפני.

✅ נכון:
```json
"title": "הכותרת החדשה שלי"
```

❌ לא נכון:
```json
"כותרת": "הכותרת החדשה שלי"
```

### ❌ אל תשנה את `_comment`

זאת הערה לעצמך, לא תוכן שמוצג באתר.

---

## מקרים מיוחדים

### מירכאות בתוך טקסט

אם יש מירכאות **בתוך** הטקסט שלך (כמו `קורס נדל"ן`), חייב לכתוב אותן כך:
```json
"title": "קורס נדל\"ן ומיסוי"
```

(הסלאש `\` לפני המירכאה אומר ל-JSON "זה לא סוף הטקסט, זו רק מירכאה רגילה").

### גרשיים עבריים

אפשר להשתמש בגרשיים עבריים `״` (חופשי, ללא בעיה):
```json
"a": "העלות היא 5,500 ש״ח"
```

### שורות חדשות בטקסט

JSON לא תומך בשבירת שורה רגילה. אם רוצים שורה חדשה בתוך טקסט:
```json
"subtitle": "שורה ראשונה\nשורה שנייה"
```

(אבל בדרך כלל לא תצטרך — הטקסט עוטף את עצמו אוטומטית).

---

## הוספה / הסרה של פריטים ברשימה

לדוגמה: רוצים להוסיף שאלה חדשה ב-FAQ.

**לפני (3 שאלות):**
```json
"items": [
  { "q": "שאלה 1", "a": "תשובה 1" },
  { "q": "שאלה 2", "a": "תשובה 2" },
  { "q": "שאלה 3", "a": "תשובה 3" }
]
```

**אחרי (הוספת שאלה 4):**
```json
"items": [
  { "q": "שאלה 1", "a": "תשובה 1" },
  { "q": "שאלה 2", "a": "תשובה 2" },
  { "q": "שאלה 3", "a": "תשובה 3" },
  { "q": "שאלה 4", "a": "תשובה 4" }
]
```

שים לב:
- כל פריט פותח ב-`{` ונסגר ב-`}`
- בין פריט לפריט יש **פסיק** (`,`)
- אחרי הפריט האחרון **אין פסיק**

---

## תצוגה מקדימה לפני פרסום (אופציונלי)

אם רוצים לראות את השינוי לפני שהוא יוצא לאוויר, אפשר:

1. בעורך GitHub, בתחתית, במקום "Commit directly to the branch" — בחר
   **"Create a new branch for this commit and start a pull request"**
2. תן לזה שם כמו `text-update-1`
3. לחץ "Propose changes"
4. Vercel יצור URL מקדים אוטומטית ויעיר אותו כתגובה ב-PR
5. בדוק את הדף בכתובת הזו
6. אם זה טוב — לחץ "Merge pull request" → "Confirm merge"
7. Vercel מעדכן את האתר הראשי

---

## שחזור גרסה קודמת (במקרה של טעות)

אם עשית טעות והאתר נשבר:

1. ב-GitHub, לך ל-`content/copy.json`
2. למעלה — לחץ על **"History"** (היסטוריה)
3. בחר את הגרסה הקודמת שעבדה
4. לחץ על הסמל של 3 הנקודות → **"Revert"**

---

## אם משהו לא עובד

- **אתר נשבר אחרי עריכה?** סימן שיש שגיאת JSON. שחזר לגרסה הקודמת (ראו למעלה).
- **שינוי לא מופיע?** המתן 90 שניות, ואז רענן (Ctrl+Shift+R לטעינה מאולצת).
- **טעות חמורה?** תכתוב לי ואני אתקן.

---

## מה אסור לערוך בקובץ הזה

הקובץ מכיל רק טקסטים. הדברים האלה **לא** ב-`copy.json` ולכן לא ניתן
לעדכן אותם דרך הקובץ:

- **צבעים, גופנים, רוחב סקציות** — צריך שינוי קוד
- **לוגו או תמונות** — קבצים נפרדים בתיקיית `public/`
- **שמות שדות הטופס** ("שם פרטי", "אימייל") — בקוד הקומפוננטה
- **שמות הקישורים בתפריט העליון** — בקוד הקומפוננטה
- **הקישורים עצמם** (ל-ishadama.co.il וכו') — בקוד הקומפוננטה

אם תרצה לשנות אחד מהאלה — תכתוב לי, אני אעשה בקוד.
